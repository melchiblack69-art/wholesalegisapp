// Canonical company-user profile page name. Kept as a re-export so existing
// imports remain compatible while routes use the clearer component name.
export { default } from "./warehouseUsersProfile";
